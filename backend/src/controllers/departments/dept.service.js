
const deptService = {
    // create or update department
    saveDept: async (req) => {

    },

    // remove department
    removeDept: async (req) => {
        
    },

    // get department list
    getDeptList: async (req) => {
        
    }
}

export default deptService